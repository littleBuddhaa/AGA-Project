{
  "name": "moodigo",
  "version": "1.0.0",
  "homepage": "http://divijapalleti.github.io/AGA-Project",
  "private": true,
  "dependencies": {
    "gh-pages": "^2.2.0",
    "node-sass": "^4.14.0",
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "^3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "b": "npm run build && serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "report-coverage": "npm test -- --coverage --watchAll=false && codecov",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
   
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/react": "^9.3.0",
    "codecov": "^3.6.1"
  }
}
